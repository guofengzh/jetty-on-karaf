patch pax-url-aether to publish MavenArtifactResolverService, which will resolve mvn url to File.

TODO: 
 locator.setServices( WagonProvider.class, new ManualWagonProvider( 5 * 1000) ); 5 * 1000 is the right timeout setting?
 